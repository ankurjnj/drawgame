services:
  - type: web
    name: drawgame
    env: node
    buildCommand: npm install && npm run build:all
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
